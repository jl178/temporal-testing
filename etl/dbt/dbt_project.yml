name: etl
version: "1.0.0"
profile: etl
model-paths: ["models"]
models:
  etl:
    +materialized: table
    # "iceberg" when the job spec has a catalog, else plain parquet tables
    +file_format: "{{ env_var('DBT_FILE_FORMAT', 'parquet') }}"
