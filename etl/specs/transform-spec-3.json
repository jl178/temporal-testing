{
  "name": "transform-spec-3",
  "project_dir": "dbt",
  "dbt_args": ["build", "--select", "tag:payments"],
  "source_table": "raw.payments",
  "require_columns": ["payment_id", "order_id", "paid_amount", "paid_date"],
  "outputs": [
    {
      "table": "analytics.fct_payments",
      "key": "curated/fct_payments.json",
      "format": "json"
    }
  ]
}
