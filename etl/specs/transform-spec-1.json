{
  "name": "transform-spec-1",
  "project_dir": "dbt",
  "dbt_args": ["build", "--select", "tag:orders"],
  "source_table": "raw.orders",
  "require_columns": ["order_id", "customer", "order_date", "amount", "status"],
  "outputs": [
    {
      "table": "analytics.daily_revenue",
      "key": "curated/daily_revenue.json",
      "format": "json"
    }
  ]
}
