{
  "name": "transform-spec-2",
  "project_dir": "dbt",
  "dbt_args": ["build", "--select", "tag:customers"],
  "source_table": "raw.customers",
  "require_columns": ["customer", "segment", "region"],
  "outputs": [
    {
      "table": "analytics.dim_customers",
      "key": "curated/dim_customers.json",
      "format": "json"
    }
  ]
}
